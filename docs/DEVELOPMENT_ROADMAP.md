# 开发路线、版本验收与团队分工

## 当前进度（0.6 Natural Voice Alpha）

`0.1–0.3` 已把“开发者工作台”收敛为普通读者会用的软件，`0.4` 承接整本书，`0.5` 已完成第一段后台生成链路，`0.6` 先解决免费试听与本地生成的自然度：

- [x] TXT 导入与 UTF-8 / UTF-16 / GB18030 检测；
- [x] 嵌套引号堆栈解析；
- [x] 千问结构化角色数据、人工别名与角色合并；
- [x] 角色 / 台词人工修改的浏览器本机持久化；
- [x] TTS 内容哈希缓存；
- [x] 部分失败保留成功缓存，重新提交时只调用失败片段；
- [x] 生成前显示未缓存字符、请求数、可选预算上限；
- [x] WAV 与可选 MP3 输出。
- [x] 默认自动选择角色分析方式，模型选择收进高级设置；
- [x] 角色卡突出系统判断、默认声音、试听和更换；
- [x] 从任意一句开始试听、倍速与当前说话人提示；
- [x] 每句提供“角色错了”“这个字读错了”“重做这句”；
- [x] 全书发音词典，朗读替换不改显示原文；
- [x] 单句生成 API，未变化句子不重复处理；
- [x] 请求数、缓存、provider 与诊断音移出普通用户主流程。
- [x] EPUB 元数据、书脊顺序、正文提取和超长章节拆分；
- [x] 多章导航与 IndexedDB 本机草稿；
- [x] `.voxcast.json` 项目导入 / 导出；
- [x] 跨章节 `CharacterRegistry` 与人工 `locked` 继承；
- [x] 旁白 + 最多 10 位主要角色，溢出人物降级但不丢台词；
- [x] 同一 CosyVoice 模型的 24 个精选音色。
- [x] 当前章节进入受限并发后台队列，服务端逐句更新状态；
- [x] 第一批句子完成后可直接连续播放，不等待整章拼接；
- [x] 章节进度、当前句、暂停、继续和部分失败补齐；
- [x] 任务 ID 随 IndexedDB 草稿保存，浏览器刷新后自动接回。
- [x] 浏览器试听优先选择标准中文人声并避开系统效果音；
- [x] 儿童、老人和成年角色只使用自然范围内的轻微节奏 / 音高差异；
- [x] Mac 使用已安装中文系统声音生成真实 WAV，并复用现有缓存与渐进播放；
- [x] 诊断音从普通用户界面和默认 API 路径移除。

仍需团队实测的不是“有没有按钮”，而是 EPUB 兼容覆盖率、中文跨章角色识别准确率、纠错耗时、真实账单误差和不同机器上的声音表现。

## 产品北极星

> 上传一本小说，自动变成多人朗读。

普通用户只需要理解六个动作：上传、识别、试听、换声音、纠错、开始生成。TTS、Token、模型、SSML、请求数和音频轨道只能出现在高级设置或开发文档中。

首版范围冻结为：

- 中文 TXT 与 EPUB；
- 旁白加最多 10 个主要角色；
- 20–30 个经过中文试听筛选的声音，而不是无限音色库；
- 自动识别、角色试听、换声音和保存角色配置；
- 单句角色纠错、全书发音纠正和单句重新生成；
- 第一章生成后即可播放，不等待全书完成；
- 不做背景音乐、多轨编辑器、声音克隆和影视级音效。

## 接下来的版本顺序

### 0.4 · 整本书基础

1. [x] EPUB 解析：目录、章节、正文和编码校验；
2. [x] 项目 JSON 导入 / 导出，保留原文、角色、别名、声音、人工锁定与发音词典；
3. [x] 跨章节 `CharacterRegistry` 基础：明确姓名 / 别名统一、人工锁定继承；
4. [x] 主要角色上限与次要角色降级策略：旁白 + 最多 10 位主要角色；
5. [x] 声音目录扩至 24 个同模型中文精选音色，并增加目录合同测试；
6. [ ] 章节级准确率评测集、真实 EPUB 兼容清单与错误类型表。

代码验收：导入一本合法 EPUB 后，切章或刷新页面时主要人物的别名、声音和人工纠正不丢失；下载项目备份后可重新打开。发布验收还需团队用至少 10 本合法 EPUB 做手工兼容测试。

### 0.5 · 边生成边播放

1. [x] 后台任务队列按当前章节、句子生成；
2. [x] 首批句子完成后立刻允许连续播放；
3. [x] 章节进度、句间暂停、继续、部分失败补齐和浏览器刷新恢复；
4. [ ] 单句版本号与原子替换：修改后播放器只替换该句；
5. [ ] 相邻同角色短句合并，减少供应商调用；
6. [ ] WebVTT / LRC 时间轴和完整章节播放器。

当前里程碑已验收：当前章不必等待全部完成，中途刷新页面能接回进度，暂停后继续会复用已生成缓存。剩余验收是把“重做这句”原子替换进正在播放的章节，并补时间轴。

### 0.6 · 自然本地语音、多供应商与质量控制

1. [x] 增加 Mac 本地中文语音 adapter，作为免费真实朗读链路；
2. [x] 浏览器和 Mac 本地声线采用自然优先映射，避开效果音与极端变调；
3. [ ] 增加另一家可商业使用的云端中文 TTS adapter；
4. [ ] 建立 provider capability matrix：语言、情绪、速率、商业授权、区域和价格；
5. [ ] 为角色保存“声音特征档案”，供应商切换时匹配最接近音色；
6. [ ] 超时、限流、熔断和自动降级；
7. [ ] 漏读、空音频、响度、采样率和串台检查；
8. [ ] 用户纠错日志只记录结构化差异，不记录密钥。

验收：主供应商不可用时任务可安全暂停或切换，不把失败音频当作完整成片；切换后主要角色仍可区分。

### 1.0 · 消费产品

1. 登录、云端项目和对象存储；
2. 平台积分、生成前预授权、成功后结算、失败释放；
3. 用量账本、免费额度、成本仪表盘和缓存淘汰；
4. 版权声明、删除与导出、数据区域选择；
5. 第三方 TTS 商业输出 / 再分发条款台账；
6. PWA 或移动端封装。

正式版默认使用平台统一 Key，用户购买应用积分，不需要理解 Token；BYOK 只作为高级选项。

## 先说结论

现场 MVP 的边界应当是：

> 一段中文小说 → 自动角色初稿 → 人工确认 → 多声线试听 / WAV。

不要在 1–2 天里做整本书、账号、付费、声音克隆和复杂混音。评委会更看重一条少而稳定的真实链路。

## 1.5 天冲刺

### D1 上午：冻结范围与数据合同

- 确认原创演示文本和三类人物。
- 固定 `CharacterProfile` 与 `ScriptSegment`。
- 画出端到端流程。
- 用 Qoder 生成测试清单和接口合同。
- 验收：CLI 能把示例拆成旁白与台词 JSON。

### D1 下午：核心系统与界面

- 完成明确发言句式。
- 完成角色卡、置信度、声线目录。
- 完成台词说话人纠错。
- 完成浏览器多人试听。
- 验收：不用 Key 也能完成现场主流程。

### D1 晚上：真实 API 与音频

- 接千问 JSON 增强。
- 接百炼非实时 TTS。
- 下载每段 WAV，统一参数后拼接。
- 加字符限额与付费确认。
- 验收：至少用真实 API 生成一次 30–45 秒缓存成片。

### D2 上午：稳定性与路演

- 跑离线测试与端到端测试。
- 保留一个“低置信度台词”展示人工纠错。
- 固定 Demo 操作序列。
- 完成 README、Qoder 记录与踩坑。
- 彩排 8 分钟路演，Demo 至少 2 分钟。

## 七人分工

| 角色 | 可见交付 | 当天验收 |
|---|---|---|
| 1. 组长 / 产品 | PRD、范围、看板、时间控制 | 没有新增失控功能 |
| 2. 文本算法 | 引号解析、发言动词、测试集 | 明确句式准确、无证据时保守 |
| 3. LLM / Prompt | 千问 JSON、人物特质、台词归属与降级 | 失败不拖垮 Demo |
| 4. TTS / 音频 | provider、WAV 下载与拼接 | 真实语音与离线模式都能跑 |
| 5. 前端 / 交互 | 角色卡、台词表、播放器 | 能改角色、能改说话人 |
| 6. 测试 / 演示 | 用例、固定数据、彩排 | 新机器从零启动成功 |
| 7. 文档 / 视觉 | README、截图、Qoder 留痕、PPT | 每个人贡献可见 |

每人都应在 Git 提交、Qoder Prompt、测试、页面或文档中留下可见贡献。

## 第 1 周：从 Demo 到 Alpha

以上代码项已在 `0.2 Alpha` 完成。实现取舍：

- 持久化已从 `localStorage` 升级到 IndexedDB，以容纳多章项目；旧浏览器保留兼容回退，不提前引入账号系统；
- 重试通过内容哈希缓存实现：重新提交同一项目时，成功片段直接命中缓存，只请求失败片段；
- 人民币费用只在配置当前供应商单价后估算，默认不硬编码易过期价格；
- MP3 使用 ffmpeg，WAV 管线仍可在纯 Python 环境运行。

验收指标：

- 明确归属台词 speaker accuracy；
- 对话边界 F1；
- 同角色声线一致率 100%；
- 失败片段可单独恢复；
- 1,000 字估算成本与实付误差可解释。

### 0.2 发布前数据验收

| 指标 | 最小样本 | 负责人输出 |
|---|---:|---|
| 明确说话人准确率 | 100 句原创 / 授权对话 | 错误类型表 + accuracy |
| 对话边界 F1 | 50 段，含嵌套与未闭合引号 | 标注 JSON + precision / recall |
| 缓存一致率 | 20 段重复生成 | provider 新调用数应为 0 |
| 失败恢复 | 人为让第 2 段失败 | 第 1 段命中缓存，仅重试第 2 段 |
| 成本误差 | 3 个百炼真实小任务 | 预估字符、控制台实付、差异说明 |

## 旧版第 2–4 周计划的状态

以下事项已重新编入上面的 `0.4–0.6`，仍按顺序做，不同时铺开：

1. 真实 EPUB 兼容清单与章节级准确率评测；
2. 已实现的人工 `locked` 继承继续做多章回归；
3. 相邻同角色短句合并，减少 TTS 请求；
4. 单句版本与章节播放列表的原子替换；
5. WebVTT / LRC 时间轴；
6. 第二家 TTS adapter 与自动降级；
7. 角色声线距离优化，避免主角听起来太像。

随后补齐：

- 专名拼音、数字和英文缩写的精细读法；
- 项目级对象存储和缓存淘汰；
- 章节级音频、封面与元数据。

## 正式产品能力

- 登录、项目与对象存储；
- 应用内 credits 与用量账本；
- BYOK 高级设置；
- 配额、并发、退避重试；
- 数据区域选择、删除与导出；
- 内容审核与版权声明；
- 监控、告警和成本仪表盘；
- 移动端或 PWA。

## 研究方向

- “角色基线 × 当前情绪”的声学控制；
- 声线可区分度的向量优化；
- 角色归属、自然度、情绪匹配与连贯性联合评测；
- 经明确授权的声音复刻。

## 每轮发布验收

- 核心链路至少有一条真实端到端测试。
- 无 Key 模式仍能运行。
- 真实 provider 可被 mock，CI 不访问外网。
- 新模型 ID 只进入配置，不散落在业务代码。
- 仓库无密钥、无受版权保护的长篇示例、无真实用户文本。
- 低置信度能被用户发现并纠正。
- 普通用户主流程不出现 Token、SSML、provider、请求数或音频轨道。
- 任何人工纠正都能明确说明影响范围：本句、全章或全书。
