# Changelog

## 0.5.0 Progressive Playback Alpha

### 用户可见

- 当前章节改为后台逐句生成，第一批句子完成后即可播放。
- 新增章节进度条、连续播放、暂停、继续和部分失败补齐。
- 任务 ID 随 IndexedDB 本机草稿保存，刷新页面后自动接回任务。
- 后台生成时可以切换章节；每章保留自己的最近任务 ID。
- 完整章节完成后仍提供统一 WAV / MP3 播放与下载。

### 工程

- 新增 `RenderJobManager` 和受限并发线程队列。
- `render_audiobook` 新增固定任务 ID、逐段进度回调和句间暂停检查。
- 新增后台任务创建、状态、暂停和继续 API。
- 任务公开快照不保存分析原文，私有 `_jobs` 目录不通过输出路由暴露。
- 新增后台进度、首批片段、暂停、缓存续跑和刷新恢复合同测试；测试增至 50 个。

## 0.4.0 Book Project Alpha

### 用户可见

- 支持导入 EPUB，读取书名、作者和书脊顺序，超长正文自动拆成可分析章节。
- 新增书籍章节导航、分析完成标记、上一章 / 下一章和当前角色容量提示。
- 跨章节保留人物 ID、别名、声音和人工锁定；旁白之外最多 10 位主要角色。
- 超出上限的新人物归入“其他角色”，保留原台词并显示可纠正提示。
- 支持 `.voxcast.json` 项目备份导入 / 导出，包含章节、角色、纠错和发音记忆。
- 多章草稿改用 IndexedDB 保存，保留 localStorage 兼容回退。
- 同一 CosyVoice 模型的精选音色从 11 个扩至 24 个。

### 工程

- 新增安全 EPUB 解包、OPF / spine 解析、正文提取和异常压缩保护。
- 新增 `BookProject`、`BookChapter` 与 `CharacterRegistry` 数据合同。
- `/api/analyze` 可接收并返回书级角色注册表；`POST /api/import/epub` 接收 EPUB 二进制。
- 测试从 33 个增加到 47 个，覆盖 EPUB、书籍项目、跨章一致性和声音目录合同。

## 0.3.0 Consumer Alpha

### 用户可见

- 主流程改为“上传 → 自动识别 → 试听 / 换声音 → 纠错 → 开始生成”。
- 分析器、导出格式和离线诊断音移入高级设置。
- 角色卡优先显示系统判断、默认声音和直接试听；证据、别名与合并能力仍保留。
- 支持 0.8–1.5 倍速、当前说话人提示和从任意一句开始试听。
- 每句话新增“角色错了”“这个字读错了”“重做这句”。
- 新增全书发音记忆；纠正一次后浏览器试听和真实合成都使用同一读法，页面原文不变。
- 生成计划改用句子、字数、已准备内容和预计费用等用户语言。

### 工程

- `AnalysisResult` 新增受限的 `pronunciations` 数据合同。
- 发音替换最长词优先、无级联，朗读文本参与缓存键。
- 新增 `POST /api/render/segment`，只生成指定句子并复用统一缓存管线。
- `/api/analyze` 新增 `auto` 模式，有千问配置时增强，否则自动使用本地基线。
- 测试从 27 个增加到 33 个，新增发音、单句生成和页面 DOM 契约覆盖。

## 0.2.0 Alpha

### 用户可见

- TXT 文件拖放与导入，支持 UTF-8、UTF-16、GB18030。
- 嵌套引号堆栈解析和未闭合引号提示。
- 角色别名编辑、重复角色合并、角色与台词人工锁定。
- 台词文本、说话人和情绪可编辑。
- 浏览器本机草稿自动保存、刷新恢复和保存副本删除。
- 生成前显示缓存命中、未缓存字符、请求数和可选费用估算。
- 内容哈希音频缓存；部分失败后只重试未成功片段。
- WAV 输出与可选 MP3 输出。
- 原文修改后阻止使用过期选角结果生成音频。

### 工程

- 测试从 12 个增加到 27 个。
- TTS provider 增加不含密钥的缓存身份合同。
- 输出缓存目录不通过 `/outputs/` 暴露。
- Docker 镜像安装 ffmpeg 以支持 MP3。

## 0.1.0 MVP

- 小说旁白 / 对话分段、说话人基线、角色卡和确定性声线匹配。
- 可选千问增强、百炼 CosyVoice HTTP provider、浏览器多人试听。
- 离线诊断音、逐段 WAV 生成、停顿拼接和下载。
