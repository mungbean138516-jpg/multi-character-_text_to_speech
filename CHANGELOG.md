# Changelog

## 0.9.0 Curated Voice Preview Alpha

### 用户可见

- 免费默认声线收敛为旁白、小女孩、小男孩、成年女性、成年男性 5 类。
- 旁白保持自然女声；成年男性从偏播报的 `Yunyang` 改为更年轻清亮的 `Yunxi`，小男孩继续使用更稚嫩的 `Yunxia`。
- 小女孩不再沿用偏成年的 `HsiaoYu`，改用更活泼的普通话 `Xiaoyi`，并采用更清晰但仍克制的童声参数；小男孩保持用户认可的配置不变。
- 成年人与旁白使用原生音高；老年角色回退为自然成年声，不再模拟夸张老人音。
- 角色卡“试听”优先生成一条 Neural 真声，与完整生成共用声线和缓存。
- 其余老人、方言和定制音色继续显示为“高级声线”：未接入付费服务时标灰，接入后可选择；产生新试听请求前会确认费用。
- 全局停止按钮现在同时停止 Neural 连续试听、设备兼容试听和角色卡 Neural / 高级试听。
- 原创演示文本改为旁白、成年女、成年男、小女孩和小男孩，不再使用老奶奶作为默认演示角色。
- 浏览器中遗留的旧版老人演示草稿会自动升级为五类免费角色演示，不影响用户自己的文本。
- “Neural 连续试听”和每句“从这里听 Neural”取代主流程中的设备鬼畜声，并与最终生成复用同一逐句缓存；设备声音仅在 Neural 不可用时兜底。
- 最终主按钮明确为“用这 5 类声音生成”，Mac 本地声音降为断网备用。
- 林夏与陈默的默认语速分别微调为 `-2%` 和 `-1%`，声线与音高保持不变，让成年角色对白更舒展自然。
- 纯英文文本自动使用对应的英文 Neural 声线；中文和中英混合内容保持现有中文声线。
- 新增 PDF / DOCX 导入、跨章节一致性检查、规则导演、情绪曲线和声线相似度预筛。

### 工程

- Neural 映射版本升级至 v7，缓存自动区分语言、旧童声、清亮成年男声和新版成年角色语速。
- `/api/config` 增加免费声线 ID、免费角色数量和声线分层能力合同。
- 增加 Neural 角色试听、脚本连续试听、免费 / 高级分层、旧演示迁移、老人自然回退、童声参数、英文路由和质量工具测试；测试增至 81 个。
- 版本升级至 0.9.0。

## 0.8.0 Mac Quick Start Alpha

### 用户可见

- Mac 用户下载 ZIP 后可直接双击 `Start-VoxCast.command`。
- 第一次运行自动选择 Python 3.10+、创建项目内独立环境并安装免费 Neural 中文声线。
- 安装成功后自动启动本地服务并打开浏览器；重复双击会直接打开已经运行的声场。
- 断网导致 Neural 安装失败时仍可启动基础版，下次联网后自动重试。

### 工程

- 启动器不使用 `sudo`，不修改系统 Python，依赖隔离在 `.voxcast-venv`。
- 增加端口、Python 版本、虚拟环境和安装失败的中文错误提示。
- 增加 Bash 语法、可执行权限、无副作用 dry-run、版本一致性和安全边界测试；测试增至 64 个。
- 版本升级至 0.8.0。

## 0.7.0 Neural Voice Pack Alpha

### 用户可见

- 新增“免费高质量生成”，安装可选声线包后无需 API Key 即可生成中文 Neural 朗读。
- 从当前 14 种在线中文声线中自动精选 9 种普通话 / 国语神经声线，方言声线不自动分配。
- 儿童与老人主要通过声线选角区分，只保留极轻的语速和音高调整。
- 保留 Mac 本地声音作为断网兜底，并明确免费 Neural 模式需要联网。

### 工程

- 新增可拔插的 `NeuralVoicePackProvider`。
- `edge-tts` 下载 MP3，`miniaudio` 解码为统一的 24kHz、16-bit、单声道 WAV。
- Neural 模式复用内容缓存、单句重做、后台任务、渐进播放和章节导出。
- 可选依赖放在 `pyproject.toml` 的 `neural` extra，基础模式仍保持零第三方依赖。
- 新增声线映射、自然变调、MP3 → WAV 和配置合同测试；测试增至 60 个。

## 0.6.0 Natural Voice Alpha

### 用户可见

- 浏览器试听优先选择标准中文人声，避开 Whisper、Zarvox 等效果音。
- 儿童、老人和成年角色的变调收窄到自然范围。
- Mac 可使用已经安装的中文系统声音免费生成、播放和导出真实 WAV。
- 用户界面不再提供诊断音乐测试音。
- 原创演示文本改为女性优先的林夏、老奶奶和小女孩。

### 工程

- 新增 `MacOSLocalTTSProvider`，使用 `say` 与 `afconvert`。
- 系统声音、语速和缓存身份采用确定性映射。
- 新增 Mac 声线解析、效果音规避和 WAV 转换测试；测试增至 55 个。

## 0.5.0 Progressive Playback Alpha

### 用户可见

- 当前章节改为后台逐句生成，第一批句子完成后即可播放。
- 新增章节进度条、连续播放、暂停、继续和部分失败补齐。
- 任务 ID 随 IndexedDB 本机草稿保存，刷新页面后自动接回任务。
- 后台生成时可以切换章节；每章保留自己的最近任务 ID。
- 完整章节完成后仍提供统一 WAV / MP3 播放与下载。

### 工程

- 新增 `RenderJobManager` 和受限并发线程队列。
- `render_audiobook` 新增固定任务 ID、逐段进度回调和句间暂停检查。
- 新增后台任务创建、状态、暂停和继续 API。
- 任务公开快照不保存分析原文，私有 `_jobs` 目录不通过输出路由暴露。
- 新增后台进度、首批片段、暂停、缓存续跑和刷新恢复合同测试；测试增至 50 个。

## 0.4.0 Book Project Alpha

### 用户可见

- 支持导入 EPUB，读取书名、作者和书脊顺序，超长正文自动拆成可分析章节。
- 新增书籍章节导航、分析完成标记、上一章 / 下一章和当前角色容量提示。
- 跨章节保留人物 ID、别名、声音和人工锁定；旁白之外最多 10 位主要角色。
- 超出上限的新人物归入“其他角色”，保留原台词并显示可纠正提示。
- 支持 `.voxcast.json` 项目备份导入 / 导出，包含章节、角色、纠错和发音记忆。
- 多章草稿改用 IndexedDB 保存，保留 localStorage 兼容回退。
- 同一 CosyVoice 模型的精选音色从 11 个扩至 24 个。

### 工程

- 新增安全 EPUB 解包、OPF / spine 解析、正文提取和异常压缩保护。
- 新增 `BookProject`、`BookChapter` 与 `CharacterRegistry` 数据合同。
- `/api/analyze` 可接收并返回书级角色注册表；`POST /api/import/epub` 接收 EPUB 二进制。
- 测试从 33 个增加到 47 个，覆盖 EPUB、书籍项目、跨章一致性和声音目录合同。

## 0.3.0 Consumer Alpha

### 用户可见

- 主流程改为“上传 → 自动识别 → 试听 / 换声音 → 纠错 → 开始生成”。
- 分析器、导出格式和离线诊断音移入高级设置。
- 角色卡优先显示系统判断、默认声音和直接试听；证据、别名与合并能力仍保留。
- 支持 0.8–1.5 倍速、当前说话人提示和从任意一句开始试听。
- 每句话新增“角色错了”“这个字读错了”“重做这句”。
- 新增全书发音记忆；纠正一次后浏览器试听和真实合成都使用同一读法，页面原文不变。
- 生成计划改用句子、字数、已准备内容和预计费用等用户语言。

### 工程

- `AnalysisResult` 新增受限的 `pronunciations` 数据合同。
- 发音替换最长词优先、无级联，朗读文本参与缓存键。
- 新增 `POST /api/render/segment`，只生成指定句子并复用统一缓存管线。
- `/api/analyze` 新增 `auto` 模式，有千问配置时增强，否则自动使用本地基线。
- 测试从 27 个增加到 33 个，新增发音、单句生成和页面 DOM 契约覆盖。

## 0.2.0 Alpha

### 用户可见

- TXT 文件拖放与导入，支持 UTF-8、UTF-16、GB18030。
- 嵌套引号堆栈解析和未闭合引号提示。
- 角色别名编辑、重复角色合并、角色与台词人工锁定。
- 台词文本、说话人和情绪可编辑。
- 浏览器本机草稿自动保存、刷新恢复和保存副本删除。
- 生成前显示缓存命中、未缓存字符、请求数和可选费用估算。
- 内容哈希音频缓存；部分失败后只重试未成功片段。
- WAV 输出与可选 MP3 输出。
- 原文修改后阻止使用过期选角结果生成音频。

### 工程

- 测试从 12 个增加到 27 个。
- TTS provider 增加不含密钥的缓存身份合同。
- 输出缓存目录不通过 `/outputs/` 暴露。
- Docker 镜像安装 ffmpeg 以支持 MP3。

## 0.1.0 MVP

- 小说旁白 / 对话分段、说话人基线、角色卡和确定性声线匹配。
- 可选千问增强、百炼 CosyVoice HTTP provider、浏览器多人试听。
- 离线诊断音、逐段 WAV 生成、停顿拼接和下载。
